# Poster Repo

This repos contains code for a web server used to upload images to an s3 bucket and returns the url to that image in the bucket.

## Running Locally

Execute the following to run in dev locally:
`uv run fastapi dev src/main.py`